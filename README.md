# MCPInspect

## Dataset
This folder contains the MCP servers we collected from six registries. For each decentralized registry, the corresponding folder includes two files: an index file listing all collected MCP servers, and a detail file containing server metadata such as descriptions and other relevant information.
